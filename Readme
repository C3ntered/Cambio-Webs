# Cambio Card Game

Cambio is a browser-based version of the memory card game where players try to
finish with the lowest-value hand. The game mixes luck, memory, and timing:
players memorize some of their cards, draw from the deck or discard pile, swap
cards, use special-card abilities, and try to eliminate matching cards when they
know what is on the table.

Live site: https://cambiogame.com

## Features

- Real-time multiplayer rooms over FastAPI WebSockets.
- Shareable room links such as `/join/ABC123`.
- Configurable starting hand size and deck count.
- Optional 60-second turn timer to prevent AFK turns from freezing a room.
- Automatic reconnection: a dropped connection or page refresh rejoins the
  same room and hand within a 90-second grace period instead of losing the
  seat.
- Single-player practice mode with an AI opponent that uses card abilities
  and calls Cambio, not just draw/swap/discard.
- Mobile-friendly table layout with opponent hands, deck/discard, and your
  hand.
- Grace-period scoring after Cambio is called.
- Stale-room cleanup to keep server memory usage low.

## Tech Stack

- Backend: Python, FastAPI, Pydantic, WebSockets.
- Frontend: HTML, CSS, and vanilla JavaScript.
- Tests: pytest for backend game and room-manager behavior.

## Project Structure

- `Backend/backend.py`: FastAPI app, REST endpoints, WebSocket handler, room
  manager, scoring, cleanup, reconnection, timers, and bot behavior.
- `Backend/test_room_manager.py`: Backend tests for deck logic, room
  management, reconnection, cleanup, scoring, and win-condition edge cases.
- `Frontend/index.html`: Main app markup and CSS.
- `Frontend/bridge.js`: Browser-side state bridge for API calls, WebSocket
  events, rendering, animations, session persistence/reconnection, and lobby
  actions.
- `Frontend/instructions.html`: Player-facing rules page.
- `Frontend/rules.pdf`: Downloadable rules reference.
- `GAME_MECHANICS_REFERENCE.md`: Gameplay rules and a code map from each rule
  to the backend/frontend code that implements it - read this first when
  reviewing a gameplay change.

## Local Development

Install backend dependencies:

```bash
python3 -m pip install -r Backend/requirements.txt
```

Run the backend and static frontend:

```bash
python3 -m uvicorn Backend.backend:app --host 127.0.0.1 --port 8000
```

Open:

```text
http://127.0.0.1:8000
```

Run tests:

```bash
python3 -m pytest Backend
```

## Room Cleanup Policy

Rooms are stored in memory, so cleanup is intentionally conservative with RAM:

- Bot-only or abandoned bot rooms close after 5 minutes.
- Playing rooms with no connected human players close after 10 minutes.
- Playing rooms with connected humans close after 20 minutes of inactivity.
- Waiting rooms with no connected humans close after 15 minutes.
- Waiting rooms with connected humans close after 45 minutes.
- Finished rooms close after 10 minutes.

The cleanup task runs once per minute.

## Credits

Created by Kai Holland.

AI assistance and co-authoring:

- OpenAI ChatGPT/Codex: code generation, optimization, documentation, and
  implementation assistance.
- Anthropic Claude: game-logic bug fixes (turn order, elimination/ability
  edge cases), reconnection support, the practice bot's ability usage and
  Cambio-calling AI, code cleanup across the backend and frontend, and
  documentation.
- Google Jules: wrote most of `Frontend/bridge.js` and assisted with game-logic
  development.

Additional tooling: Cursor was used during development for editing and AI-assisted
iteration.

## Lessons Learned

This project was a practical way to connect Python backend code with a browser
frontend through WebSockets. The most useful lessons were around keeping server
state authoritative, validating every gameplay action on the backend, designing
cleanup for an in-memory room system, and reviewing AI-generated code carefully
before shipping it.
